// 3. Explain the output of the following code snippet and why,
console.log(foo);
var foo = "Hello, World!";
console.log(foo);
//Ans:- javascript is loosely type language so we can change the value of a defined variable using(var) but can't chnage if we use const.
